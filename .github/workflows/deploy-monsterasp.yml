# EventSphere API -> MonsterASP deploy pipeline
name: Deploy API to MonsterASP

on:
  push:
    branches: [main]
    paths:
      - 'server/**'
      - '.github/workflows/deploy-monsterasp.yml'
  workflow_dispatch:

concurrency:
  group: monsterasp-deploy
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    name: Publish + FTP deploy
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server/EventSphere.Server
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Publish (framework-dependent)
        run: dotnet publish -c Release -o publish --no-self-contained

      - name: Write production appsettings
        shell: bash
        env:
          DB: ${{ secrets.PROD_DB_CONNECTION }}
          ORIGIN: ${{ secrets.PROD_CORS_ORIGIN }}
          JWT: ${{ secrets.PROD_JWT_KEY }}
          PM: ${{ secrets.PROD_PAYMONGO_SECRET }}
        run: |
          jq -n \
            --arg db "$DB" \
            --arg origin "$ORIGIN" \
            --arg jwt "$JWT" \
            --arg pm "$PM" \
            '{ConnectionStrings:{DefaultConnection:$db},Cors:{AllowedOrigins:[$origin]},Jwt:{Issuer:"EventSphere.Api",Audience:"EventSphere.Client",Key:$jwt,ExpiryMinutes:480},PayMongo:{BaseUrl:"https://api.paymongo.com/v1",SecretKey:$pm},Logging:{LogLevel:{Default:"Information","Microsoft.AspNetCore":"Warning"}},AllowedHosts:"*"}' \
            > publish/appsettings.Production.json
          echo "appsettings.Production.json generated"

      - name: Configure web.config (force Production environment)
        shell: bash
        run: |
          python3 - <<'PY'
          import re, xml.dom.minidom
          p = "publish/web.config"
          with open(p, "r", encoding="utf-8") as f:
              c = f.read()
          tag = re.search(r"<aspNetCore[^>]*/>", c)
          if not tag:
              raise SystemExit("aspNetCore tag not found")
          attrs = tag.group(0).rstrip("/>").rstrip()
          new_tag = attrs + ">\n" + \
              '    <environmentVariables>\n' + \
              '      <environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Production" />\n' + \
              '    </environmentVariables>\n' + \
              '  </aspNetCore>'
          c = c[:tag.start()] + new_tag + c[tag.end():]
          with open(p, "w", encoding="utf-8") as f:
              f.write(c)
          # fail the build loudly if the result is not well-formed XML
          xml.dom.minidom.parse(p)
          print("web.config patched and VALID")
          PY

      - name: Take app offline
        shell: bash
        run: |
          printf '%s' '<!DOCTYPE html><html><body>Server updating - please wait.</body></html>' > app_offline.htm
          curl -sS --connect-timeout 30 --max-time 60 \
            --user "${{ secrets.FTP_LOGIN }}:${{ secrets.FTP_PASSWORD }}" \
            -T app_offline.htm \
            "ftp://${{ secrets.FTP_HOST }}/wwwroot/app_offline.htm"

      - name: Wait for worker shutdown
        shell: bash
        run: sleep 5

      - name: Upload application files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_LOGIN }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: server/EventSphere.Server/publish/
          server-dir: ./wwwroot/
          protocol: ftp
          dangerous-clean-slate: false
          exclude: '**/logs/**'
          timeout: 300000

      - name: Bring app back online
        shell: bash
        run: |
          curl -sS --connect-timeout 30 --max-time 60 -o /dev/null \
            --user "${{ secrets.FTP_LOGIN }}:${{ secrets.FTP_PASSWORD }}" \
            "ftp://${{ secrets.FTP_HOST }}/" \
            --quote "CWD /wwwroot" \
            --quote "DELE app_offline.htm" \
            && echo "app_offline.htm removed" \
            || { echo "ERROR: could not remove app_offline.htm - site stays offline"; exit 1; }

      - name: Smoke test API
        continue-on-error: true
        shell: bash
        run: |
          code=$(curl -sS -o /dev/null -w "%{http_code}" --max-time 90 \
            http://eventsphere-backend.runasp.net/api/events/public || echo "000")
          echo "GET http://eventsphere-backend.runasp.net/api/events/public -> HTTP $code"